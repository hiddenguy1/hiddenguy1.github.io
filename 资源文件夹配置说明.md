# Hexo 文章资源文件夹配置说明

## 配置完成情况

### 1. 已启用的配置

在 `_config.yml` 中已设置：

```yaml
post_asset_folder: true
```

### 2. 已安装的插件

`hexo-asset-image@1.0.0` - 自动处理图片路径的插件

### 3. 已更新的文章模板

`scaffolds/post.md` 已更新，包含图片引用说明

## 功能特性

### 自动创建资源文件夹

创建新文章时，Hexo 会自动创建同名资源文件夹：

```bash
hexo new "文章标题"
```

会生成：
- `source/_posts/文章标题.md` - 文章文件
- `source/_posts/文章标题/` - 资源文件夹

### 支持的资源类型

- 图片：`.jpg`, `.png`, `.gif`, `.svg`, `.webp`
- 文档：`.pdf`, `.doc`, `.docx`
- 数据文件：`.xlsx`, `.csv`, `.json`
- 压缩包：`.zip`, `.rar`
- 其他媒体文件

## 图片引用方式

### 方式 1: 标准 Markdown 语法（推荐）

```markdown
![图片描述](图片名.jpg)
```

**优点**：
- 语法简洁
- Markdown 标准语法
- 兼容性好

### 方式 2: 使用 asset_img 标签

```markdown
{% asset_img 图片名.jpg 图片描述 %}
```

**优点**：
- 自动处理路径
- 支持更多参数（如 width, height）
- 需要安装 `hexo-asset-image` 插件

### 方式 3: 相对路径引用

```markdown
![图片描述](./文章标题/图片名.jpg)
```

**注意**：需要确保路径正确

### 方式 4: HTML 标签

```html
<img src="/文章标题/图片名.jpg" alt="图片描述">
```

**优点**：
- 完全控制
- 支持所有 HTML 属性

## 使用示例

### 创建带图片的文章

1. 创建文章：
   ```bash
   hexo new "旅行日记"
   ```

2. 将图片放入资源文件夹：
   ```
   source/_posts/旅行日记/
   ├── photo1.jpg
   ├── photo2.jpg
   └── map.png
   ```

3. 在文章中引用：
   ```markdown
   # 旅行日记

   第一站的照片：
   ![第一站](photo1.jpg)

   第二站的照片：
   ![第二站](photo2.jpg)
   ```

### 生成和部署

生成静态网站时，资源会自动复制到正确的位置：

```bash
hexo generate
hexo deploy
```

## 文件结构示例

```
source/_posts/
├── 图片使用示例.md
├── 图片使用示例/
│   ├── demo.jpg
│   ├── code.pdf
│   └── README.md
├── 旅行日记.md
└── 旅行日记/
    ├── photo1.jpg
    ├── photo2.jpg
    └── map.png
```

## 最佳实践

### 1. 文件命名规范

- 使用小写英文字母
- 避免使用空格和特殊字符
- 使用有意义的名称
- 例如：`sunset-view.jpg` 而不是 `IMG_001.jpg`

### 2. 图片优化

- 压缩图片以减小文件大小
- 使用合适的格式（WebP 优于 PNG）
- 控制图片尺寸

### 3. 文件夹管理

- 每篇文章使用独立的资源文件夹
- 不要跨文章共享资源文件夹
- 定期清理未使用的资源

### 4. 备份策略

- 资源文件夹是博客内容的一部分
- 提交到版本控制时包含资源文件夹
- 部署时确保资源文件正确上传

## 常见问题

### Q1: 图片无法显示？

**解决方法**：
1. 检查图片文件名是否正确
2. 确认图片在资源文件夹中
3. 检查文件扩展名大小写
4. 清除缓存：`hexo clean && hexo generate`

### Q2: 如何引用其他文章的资源？

**不推荐**跨文章引用资源。如果需要共享资源，考虑：
- 将公共资源放在 `source/images/` 目录
- 使用绝对路径引用

### Q3: 资源文件夹未自动创建？

**检查**：
1. 确认 `post_asset_folder: true` 已配置
2. 重新运行 `hexo new` 命令
3. 手动创建同名文件夹也可以

## 相关文档

- [Hexo 官方文档 - 资源文件夹](https://hexo.io/zh-cn/docs/asset-folders)
- [hexo-asset-image 插件文档](https://github.com/CodeFalling/hexo-asset-image)

## 配置文件位置

- 主配置：`f:/hexo_blog/hexo-source/_config.yml`
- 文章模板：`f:/hexo_blog/hexo-source/scaffolds/post.md`
- 示例文章：`f:/hexo_blog/hexo-source/source/_posts/图片使用示例.md`

## 更新日期

2026-02-25
